server {
    listen 80;
    server_name localhost;
    client_max_body_size 0;
    root /usr/share/nginx/html;
    index index.html;

    # Los usuarios se crean desde el panel de administración.
    location ~* ^/api/auth/register(?:/|$) {
        default_type application/json;
        return 403 '{"success":false,"error":{"code":"REGISTRATION_DISABLED","message":"El registro público está deshabilitado."}}';
    }

    location /api/ {
        proxy_pass http://alux-backend-dnsz1q:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        proxy_pass http://alux-backend-dnsz1q:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Bundles con hash: nunca devolver index.html (evita error MIME en module scripts)
    location /assets/ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files $uri =404;
    }

    location = /sw.js {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
